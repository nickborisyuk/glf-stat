{
  "name": "glf-stat",
  "version": "1.0.0",
  "description": "Golf Statistics Tracking App",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "cd server && npm install && npm start",
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
